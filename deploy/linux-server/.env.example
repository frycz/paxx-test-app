# Deployment environment variables for paxx-test-app
# Copy to .env and customize for your server:
#   cp .env.example .env

# =============================================================================
# REQUIRED - Update these before deploying
# =============================================================================

# Docker image (update with your GitHub username/org)
IMAGE=ghcr.io/YOUR_USERNAME/paxx-test-app:latest

# PostgreSQL password (use a strong, random password in production)
POSTGRES_PASSWORD=changeme

# =============================================================================
# OPTIONAL - Defaults are usually fine
# =============================================================================

# App environment
APP_ENV=production

# Health check timeout (seconds to wait for new container to be healthy)
HEALTH_TIMEOUT=60

# Drain timeout (seconds for graceful shutdown of old container)
DRAIN_TIMEOUT=30
